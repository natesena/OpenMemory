{
    "name": "openmemory",
    "private": true,
    "scripts": {
        "install": "cd backend && npm install",
        "build": "cd backend && npm run build",
        "start": "cd backend && npm start"
    },
    "devDependencies": {
        "@nathanielsena/claude-commands": "github:natesena/claude-commands"
    }
}
